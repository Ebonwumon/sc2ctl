:q
@if ($errors->count() > 0)
    <div class="errors" style="display:block;">
      <ul>
        @foreach($errors->all() as $message)
          <li>{{ $message }}</li>
        @endforeach
      </ul>
    </div>
@endif
